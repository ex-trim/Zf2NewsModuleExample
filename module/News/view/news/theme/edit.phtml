<?php
$title = 'Edit theme name';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>
<p><a href="<?= $this->url('themes'); ?>"><< back</a></p>
<?php
$form = $this->form;
$form->setAttribute('action', $this->url('themes/edit', array(), true));
$form->prepare();

$form->get('submit')->setValue('Update theme');

echo $this->partial('_form.phtml', array('form' => $form)); ?>